# Claude Session — 2026-02-28

## Project: rodmachen.com

### Topic: Representing external publications on the site

User has stories published at Austin Chronicle (~140 articles) and Cinapse (~218 articles). Explored options for representing these on the site without copying content. Discussed five approaches: clips page with links, new content collection, publication landing pages, unified writing index, and curated highlights.

**Resolution:** Chose a new `bylines` content collection with manual markdown files. Each file contains frontmatter (title, subtitle, publication, url, date, tags) and the first paragraph of the published article as body content, ending with a "Read more at {publication}" link.

### Topic: Integrating bylines into the convert-to-blog plan

Incorporated the bylines feature into the existing `docs/plans/convert-to-blog.md` as Phase 6. Key decisions:

- **Collection name:** `bylines` (user preferred "byline" over "clips")
- **Navigation:** Renamed Blog to Writing, combining existing posts and external bylines into one unified hub
- **Categories updated:** Added `essay` as a new category/template alongside article, newsletter, review, and byline
- **Subtitles:** Added `subTitle` field to bylines schema (matching existing posts schema). Fetch scripts extract subtitles from both sources. Substack migration also captures subtitles.

### Topic: Scaling from ~25 to ~350+ byline files

Initial estimate of 20-25 bylines was corrected — Austin Chronicle has ~140 articles (12 pages) and Cinapse has 218 posts. Updated plan to use automated fetch scripts instead of manual creation:

- **`scripts/fetch-cinapse-bylines.mjs`** — Uses WordPress REST API (author ID 32, two-pass download matching cinapse-intelligence project approach). Extracts first paragraph from `content.rendered` HTML.
- **`scripts/fetch-chronicle-bylines.mjs`** — Two-step scrape: paginate author listing for URLs, then fetch each article page for subtitle and first paragraph. Uses cheerio for HTML parsing.
- **Idempotency** built in from the start — scripts check for existing files before writing, safe to re-run for incremental updates.

Referenced `/Users/rodmachen/code/cinapse-intelligence/cinapse-recs-plan.md` for WordPress API patterns.

### Topic: Removing duplicate published posts

Posts in `src/content/posts/` with `published: true` in frontmatter are copies of externally published pieces. These will be deleted in Phase 6 step 6d since they'll be replaced by byline entries linking to the originals. Only unpublished/original posts remain as articles.

### Topic: AI-powered tag generation

Added Phase 7 to the plan: a script (`scripts/generate-tags.mjs`) that uses Claude Haiku to assign tags from a controlled vocabulary to all content (~400+ files across posts and bylines). Includes dry-run mode. Cost under $1. Controlled vocabulary ensures consistency (e.g., "film" not "movies"/"cinema").

### Topic: Read More links in byline body content

Updated the byline markdown format to include a `[Read more at {publication} →](url)` link baked into the markdown body by the fetch scripts. Applies to all bylines from both Cinapse and Chronicle. Renders naturally on listing pages without special template logic.

---

### Topic: Fix tag generation script bugs

Fixed two bugs in `scripts/generate-tags.mjs`:
1. **Markdown code fences:** Claude Haiku wraps JSON responses in `` ```json ... ``` ``, causing `JSON.parse` to fail. Added regex to strip fences before parsing.
2. **Missing files:** Script crashed with ENOENT on deleted files. Added `fs.existsSync()` check to skip gracefully.

Also added **529 (overloaded) error handling** — retries after 30 seconds, same as 429 rate limiting.

Added **skip-already-tagged** logic so re-runs don't re-process files that already have tags, allowing the script to resume where it left off.

### Topic: Flatten multi-category posts to single category

Found 7 posts with `category` as a YAML array (all resolving to `article`). Flattened all to `category: article` as a simple string. Three SXSW posts that had `sxsw` as a second category value will use tags instead. Confirmed that changing the `category` field in frontmatter is all that's needed to move a post between categories.

### Topic: Add subtitles to homepage category cards

Implemented subtitle display on homepage category cards (option 3: subtitle as a second line in smaller/lighter italic text). Updated the `GridCard` type, data mapping for both posts and bylines, and added `.item-subtitle` CSS styling.

### Topic: Add subtitles to all listing pages

Extended subtitle display to all listing pages:
- `src/pages/[category]/index.astro` — category listing pages
- `src/pages/byline/index.astro` — bylines listing
- `src/pages/topics/[tag].astro` — individual topic pages
- `src/pages/archive/[...page].astro` — archive pages

All use consistent styling: `font-size: 0.8rem`, `color: var(--color-text-light)`, `font-style: italic`.

---

### Topic: DRY refactoring — shared components, utilities, and CSS variables

Implemented a comprehensive refactor to consolidate duplicated code across 4 listing pages (archive, topics/[tag], [category]/index, byline/index):

- **Created `src/components/PostList.astro`** — Reusable post-list component with date + title + subtitle + publication + tags layout
- **Created `src/components/PageHeader.astro`** — Shared header with title, subtitle, count props
- **Added `ListItem` type, `formatDate()`, `postToListItem()`, `bylineToListItem()`** to `src/utils/posts.ts`
- **Added CSS variables** `--color-bg-muted` and `--color-bg-muted-hover` to `src/styles/global.css`, replacing hardcoded `#f0f0f0`/`#e0e0e0`
- **Extracted shared `tagTransform`** in `src/content.config.ts` to DRY up posts/bylines schemas
- **Simplified all listing pages** to use shared components — archive went from 234 to 48 lines, byline from 128 to 25 lines

### Topic: Add tags to all listing pages (remove category badges)

Changed `PostList.astro` to show tags by default (`showTags` defaults to `true`). Removed `showCategoryBadge` prop and its CSS entirely. All listing pages now show tags automatically.

### Topic: Horizontal card layout for category and topic listing pages

Created `src/components/PostCardList.astro` — a new card-based layout with a square image on the left and post info on the right. Cards have border, rounded corners, and hover effects. Fallback for posts without images is a category-colored square showing the first letter of the title. Applied to `[category]/index.astro` and `topics/[tag].astro`. Archive and byline pages kept the original `PostList` layout.

### Topic: Extract first image from post markdown for card thumbnails

Added `extractFirstImage()` utility to `src/utils/posts.ts` that parses raw markdown body to find the first `![...](/images/...)` pattern. Handles markdown title attributes (`"title"` after the URL). Newsletter posts with embedded images now show photo thumbnails in the card layout; posts without images fall back to the colored initial square.

### Topic: Fix Substack image-link rendering artifacts

Fixed 52 broken image renders across 13 newsletter posts. Substack exports wrapped images in link syntax: `[` (newline) `![alt](/local-image)` (newline) `](https://substackcdn.com/...)` — the newlines broke markdown rendering, showing literal `[` and `](url)` text on the page. Wrote a Python script to strip the wrapping link syntax, leaving clean `![alt](/images/...)` tags.

### Topic: Fix extractFirstImage regex for images with title attributes

The regex `[^)]+` was capturing the markdown title attribute (e.g., `"The Title"`) as part of the image URL, breaking `<img src>`. Fixed to `\S+?` with an optional `(?:\s+"[^"]*")?` group to properly separate path from title.

### Topic: Publication logos for byline cards

Switched byline page from `PostList` to `PostCardList` layout. Added `PUBLICATION_LOGOS` mapping in `src/utils/posts.ts` to associate publication names with logo image paths. User provided square logos for Austin Chronicle (`/images/austin-chronicle.png`) and Cinapse (`/images/cinapse.png`). Byline cards now show publication logos as the square card image.

---

### Topic: Deploy to Vercel

Deployed the site to Vercel following the deployment plan (`docs/plans/cheeky-jumping-bumblebee.md`).

**Key steps completed:**
1. Set `PUBLIC_CLOUDINARY_CLOUD_NAME=dke4phurv` in `.env`
2. Installed Vercel CLI globally, logged in, and linked project
3. Renamed Vercel project from `rodmachen.com` to `edition-rodmachen-com` to reserve `rodmachen.com` for a future homepage
4. Set `PUBLIC_CLOUDINARY_CLOUD_NAME` env var in Vercel (production)
5. Deployed via `vercel deploy --prod` — 73 pages built successfully
6. Added `edition.rodmachen.com` custom domain with A record `76.76.21.21` in AWS Route 53 (replaced existing CNAME)
7. Verified homepage, OG tags, and redirects

### Topic: Create edition-rodmachen-com GitHub repo

Created new GitHub repo `rodmachen/edition-rodmachen-com` and pushed full history. Initial push was blocked by GitHub push protection (old AWS keys in `s3_website.yml` from Jekyll era) — resolved by allowing the secrets via GitHub's unblock URLs. Added `edition` as a git remote alongside `origin` (still pointing to `rodmachen/rodmachen.com`).

### Topic: Fix trailing-slash redirect for /categories/

`/categories/film/` (with trailing slash) wasn't redirecting while `/categories/film` worked. Fixed by adding a separate redirect rule in `vercel.json` with trailing slash pattern (`/categories/:path*/` → `/topics/:path*/`) before the non-trailing-slash rule. Redeployed to production.
